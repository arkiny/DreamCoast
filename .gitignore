# Rust build artifacts
/target

# Vendored shader compiler binaries (not committed; obtain separately)
/tools/slang/

# Vulkan validation layer binaries (fetched by tools/fetch-vulkan-layers.py; not committed)
/tools/vulkan-layers/

# Compiled shader artifacts — local-only cook cache (Phase 12 M4.2; build.rs writes
# per-OS bytecode + manifest here and embeds via include_bytes!). Not committed (§5).
/crates/shader/compiled/

# Downloaded sample assets (fetched at runtime; cube fallback otherwise)
/assets/

# Cooked-asset cache (Phase 12 M1; .dcasset binaries cooked from glTF on a cache
# miss). Local-only, re-cooked on demand — not committed (§ M4-aligned policy).
/cache/

# Screenshot output (--screenshot[/-clean] flags, F2 captures)
/tmp/
/screenshot_*.png

# IDE / OS
.vscode/
.idea/
*.pdb
Thumbs.db

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# DirectX Shader Compiler build (Track 2 RT pipeline toolchain; not committed)
/tools/dxc-src/
/tools/dxc/

# RenderDoc MCP graphics-debugging server (set up by tools/setup-renderdoc-mcp.ps1):
# cloned upstream repo, the Python 3.12 venv, and the rebuilt renderdoc.pyd module.
# All local-only — runs as a SEPARATE process, never linked into the engine.
/tools/renderdoc-mcp/
# RenderDoc source checkout used by tools/build-renderdoc-py312.ps1 to produce a
# Python-3.12-compatible renderdoc.pyd (the official Windows module is 3.6-only).
/tools/renderdoc-src/
